// jest.setup.js
import { vi } from 'vitest';

vi.mock('../src/services/openaiConnector.js', async () => ({
  callOpenAI: async (prompt) => {
    console.log('[🧪 MOCKED OPENAI]', prompt.slice(0, 80) + '...');
    return JSON.stringify({
      decisao: "OPERAR",
      justificativa: "Mocked response (vi.mock)",
      sizing: "5%",
      tp: "3%",
      sl: "-1.8%",
      modo: "testnet"
    });
  }
}));
