# Grafana Datasource Configuration
# Simple configuration file for Prometheus datasource

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: http://prometheus:9090
    access: proxy
    isDefault: true
