[providers]
node = "18"

[phases.setup]
nixPkgs = ["nodejs-18_x", "npm"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "cd api-gateway",
  "npm install --production"
]

[phases.start]
cmd = "cd api-gateway && npm start"

[variables]
NODE_ENV = "production"
PORT = "3000"
