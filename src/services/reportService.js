// Função de relatório, para uso com jobs de fechamento, e-mail ou WhatsApp
export async function sendReport(type) {
  // type: 'asia_open', 'asia_close', 'us_macro'
  console.log(`[Report] sendReport called for ${type}`);
  // Implementar lógica de geração/envio real (e-mail, WhatsApp, dashboard, etc.)
}
