export async function fetchAndSaveFearGreed() {
  // Mock simples (substitua pela chamada real depois)
  return { ok: true, msg: 'Fear & Greed Index mockado', value: 56, date: new Date().toISOString() };
}
