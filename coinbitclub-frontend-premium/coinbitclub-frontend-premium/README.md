# CoinbitClub Frontend Premium

Next.js + TypeScript application consuming the CoinbitClub Market Bot backend.

## Development
```bash
# install deps (requires internet)
yarn install
# lint and run tests
yarn lint
yarn test:unit
# run dev server
yarn dev
# build for production
yarn build && yarn start
